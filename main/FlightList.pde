// KW
/*
class FlightList extends Flight{
  
  ArrayList<Flight> flightList = flights;
  Table table = new Table();
  table.addColumn("id");
  table.addColumn("carrierCode");
  table.addColumn("flightNum");
  TableRow newRow = table.addRow();
  
  FlightList(String flightDate, String carrierCode, int flightNum, String origin, 
  String originCity, String dest, String destCity, String schDepTime, String depTime, 
  String schArrTime, String arrTime, int cancelled, int diverted) {
  super(flightDate, carrierCode, flightNum, origin, originCity, dest, destCity, schDepTime, 
  depTime, schArrTime, arrTime, cancelled, diverted);
  
  newRow.setInt("id", table.lastRowIndex());
  newRow.setString("carrierCode", carrierCode);
  newRow.setInt("flightNum", flightNum);
}

void draw(){
  table.loadTable();
}
*/
